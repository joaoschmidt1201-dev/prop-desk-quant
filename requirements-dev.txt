# ─────────────────────────────────────────────────────────────────────────────
# Dev tooling — linting, type checking, testing, pre-commit
# Instalar localmente: pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────
-r requirements.txt

# Linting + formatting
ruff>=0.8

# Type checking
mypy>=1.14
pandas-stubs
types-requests
types-pytz
types-openpyxl

# Testing
pytest>=8.0
pytest-cov>=5.0

# Pre-commit hooks
pre-commit>=4.0
